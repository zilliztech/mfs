"""Engine components: repositories, state machines, and infrastructure helpers.

``ObjectRepository`` (objects / object_tasks / connector_jobs / connectors table
SQL + task/job state machine) lives here.
"""
